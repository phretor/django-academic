Provides handy template tags and filters such as

    {{ object|protags_permalink object [suffix] [template_name] }}

to create a URI out from the object's get_absolute_url and an optional
[suffix] string.
